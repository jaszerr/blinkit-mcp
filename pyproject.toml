[project]
name = "blinkit-mcp"
version = "1.0.2"
description = "Automate grocery shopping on Blinkit directly from Claude Desktop. Search, cart, and UPI payments."
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["playwright>=1.57.0", "mcp>=1.25.0", "python-dotenv>=1.2.1"]
